---
# roles/iscsi-target/defaults/main.yml — iSCSI target role defaults

# CHAP authentication for backend replication (between storage nodes)
iscsi_chap_enabled: true
iscsi_mutual_chap_enabled: true

# Credentials — set in group_vars/storage_nodes/iscsi.yml (vault in production)
# iscsi_chap_user: ""
# iscsi_chap_password: ""
# iscsi_mutual_chap_user: ""
# iscsi_mutual_chap_password: ""
